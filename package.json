{
  "name": "cryptovoxels-parcels",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ benjythebee/cryptovoxels-parcels",
    "create-local": "graph create --node http://localhost:8020/ benjythebee/cryptovoxels-parcels",
    "remove-local": "graph remove --node http://localhost:8020/ benjythebee/cryptovoxels-parcels",
    "deploy-local": "graph deploy --product hosted-service --node http://localhost:8020/ --ipfs http://localhost:5001 benjythebee/cryptovoxels-parcels"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.28.1",
    "@graphprotocol/graph-ts": "^0.26.0"
  }
}
